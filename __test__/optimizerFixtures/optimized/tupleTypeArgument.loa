use Loa.Core.Unit
use Loa.Core.Tuple2
use Loa.Core.Tuple3
use Loa.Core.Tuple4
use Loa.Core.Tuple5

public f () -> Unit { return () }
public f () -> T { return () }
public f () -> Tuple2<T, U> { return () }
public f () -> Tuple3<T, U, V> { return () }
public f () -> Tuple4<T, U, V, X> { return () }
public f () -> Tuple5<T, U, V, X, Y> { return () }
